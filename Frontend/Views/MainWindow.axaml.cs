using Avalonia.Controls;

namespace Frontend.Views;

/// <summary>
/// The main application window.
/// Serves as the root container for the app and hosts the primary views.
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
